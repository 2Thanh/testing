import pygame
import neat
pygame.init()
WIDTH = 800
HEIGHT =600
BLACK = (0,0,0)
WHITE = (255,255,255)

screen = pygame.display.set_mode((WIDTH,HEIGHT))
pygame.display.set_caption("Dinosaur Google Game")

running = True
clock = pygame.time.Clock()

while running :
    if (ev)